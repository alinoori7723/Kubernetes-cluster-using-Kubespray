wordpress:
  replicaCount: 1
  image:
    repository: wordpress
    tag: 5.8.0-apache
    pullPolicy: IfNotPresent
  service:
    type: ClusterIP
    port: 80
  ingress:
    enabled: true
    hosts:
      - host: "alinoori.maxtld.dev"
        paths:
          - path: /wordpress
            pathType: Prefix

mysql:
  replicaCount: 1
  image:
    repository: mysql
    tag: 5.7
    pullPolicy: IfNotPresent
  rootPassword: "rootpassword"
  database: "wordpress"
  user: "wpuser"
  password: "wppassword"
  service:
    type: ClusterIP
    port: 3306

phpmyadmin:
  replicaCount: 1
  image:
    repository: phpmyadmin/phpmyadmin
    tag: latest
    pullPolicy: IfNotPresent
  service:
    type: ClusterIP
    port: 80
  ingress:
    enabled: true
    hosts:
      - host: "alinoori.maxtld.dev"
        paths:
          - path: /dbadmin
            pathType: Prefix

